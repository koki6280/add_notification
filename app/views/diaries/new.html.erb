<div class="container" style="margin-top: 100px;">
	<div class="row">
        <h2>新規投稿ページ</h2>
        <div class="col-xs-8">
                <%= form_for(@diary) do |f| %>
            <div class="field">
            	<%= f.label :tag_list, 'タグ  (タグはカンマ区切りで複数入力できます)' %><br>
            	<%= f.text_field :tag_list, size: "90", value: @diary.tag_list.join(',') %>
            </div>

            <div class="field">
                <%= f.label :body, '本文' %><br>
                <%= f.text_area :body, size: "90x5", class: "diary_body" %>
            </div>
            <h4>本日の実績を入力してください！　※飲酒量は350ml缶で1とします</h4>
            <div class="field" style="margin-bottom: 5px;">
            	<%= f.number_field :exercise , placeholder: "運動時間" %>
            </div>

            <div class="field" style="margin-bottom: 5px;">
            	<%= f.number_field :sleep , placeholder: "睡眠時間" %>
            </div>

            <div class="field" style="margin-bottom: 5px;">
            	<%= f.number_field :cigarette , placeholder: "喫煙本数" %>
            </div>

            <div class="field">
            	<%= f.number_field :drinking , placeholder: "飲酒量" %>
            </div>
        </div>

        <div class="col-xs-3">
            <div class="field">
                <%= f.label :image, 'メディア' %>
                <%= f.attachment_field :body_image, class: "body_image" %>
            </div>

            <div class="field">
            	<%= f.submit '投稿' %>
            </div>
        </div>

        <% end %>
    </div>
</div>
